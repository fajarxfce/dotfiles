# ── Hyprland core ──────────────────────────────
hyprland
xdg-desktop-portal-hyprland
hyprlock
hypridle
hyprpolkitagent
# ── Bar / launcher / notify / wallpaper ────────
waybar
wofi
swaybg
mako
# ── Wayland utilities ──────────────────────────
wl-clipboard
cliphist
grim
slurp
swappy
brightnessctl
playerctl
wireplumber
wlr-randr
# ── Search / system info / calendar ────────────
fd
fzf
btop
gsimplecal
# ── File manager + terminal ────────────────────
# Needed by bin/thunar-actions.sh ("Open Terminal Here" → alacritty)
thunar
thunar-volman
gvfs
alacritty
kitty          # sekunder: theme.sh masih menjaga warnanya
# ── Network & Bluetooth (easy toggles) ─────────
network-manager-applet
blueman
pavucontrol
# ── Toolkits / theming / fonts ─────────────────
qt5-wayland
qt6-wayland
gnome-themes-extra
# Vulkan driver for the Intel iGPU. Without it the ONLY Vulkan device is the
# NVIDIA dGPU, so GPU apps like Zed get pinned to the RTX 3060 (30 W idle, stalls
# on refocus). With it we can force Zed onto the iGPU (see bin/zeditor).
vulkan-intel
ttf-jetbrains-mono-nerd
otf-font-awesome
xdg-user-dirs
